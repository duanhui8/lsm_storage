FILE(GLOB_RECURSE ROOTSERVER_SRC *.cpp)
ADD_LIBRARY(rootserver STATIC ${ROOTSERVER_SRC})
TARGET_INCLUDE_DIRECTORIES(rootserver PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/..)
TARGET_LINK_LIBRARIES(rootserver share common)
